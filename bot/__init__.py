"""প্রবাহ — Professional Ultra Quiz Bot (modular package).

The bot's behaviour is identical to the original single-file script.
Code is split into ordered sections under ``bot/sections/`` and executed
in the original order inside a shared namespace by ``bot/__main__.py``.

Run with:  ``python -m bot``
"""